<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/main.css">
    <title>Form</title>
</head>
<body>
    <div class="container2">
        <h1>
          <%= heading %>
        </h1>
    
        <% if (locals.content) { %>
            <form id="editForm" method="post" action="/books/<%=content.id%>">
                <input type="text" name="title" value="<%=content.title %>">
                <textarea name="content" rows="10"><%=content.content %></textarea>
                <input type="text" name="rating" value="<%=content.rating %>">
                <input type="text" name="isbn" value="<%= content.isbn %>">
                <button class="full-width" type="submit">
                    <%= submit %>
                </button>
            </form>
          <% } else {%>
            <form id="newPostForm" method="post" action="/books">
                <input type="text" name="title" placeholder="Title" required>
                <textarea name="content" placeholder="Content" required rows="10"></textarea>
                <input type="text" name="rating" placeholder="Rating" required>
                <input type="text" name="isbn" placeholder="ISBN" required>
                <button class="full-width" type="submit">
                    <%= submit %>
                </button>
            </form>
        <% } %>
    
    </div>
</body>
</html>